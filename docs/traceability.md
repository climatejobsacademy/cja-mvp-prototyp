# Traceability-Tabelle — AfCJ MVP-Prototyp

Quelle: Definition of Done (`definition-of-done.md`), Punkt „Die Traceability-Tabelle ist aktualisiert", und Playbook Schritt 6a/6d. Bildet ab, welcher PR welche SR-xx (aus `requirements.md`) abdeckt. Stand 2026-09-11 — erster Entwurf, siehe Hinweis unten.

Wichtiger Vorbehalt: Kein PR ist bisher gemergt (PR #1 und PR #2 warten beide auf Maltes Review). Der Status hier beschreibt also, was in offenen PRs vorbereitet ist, nicht was in `main` bereits verifiziert läuft. Die Zuordnung SR → PR ist für die unten gelisteten Zeilen aus dem bisherigen Gesprächsverlauf begründet (Migrationen, RLS-Tests, Access-Matrix-Zeilen); für alle anderen SRs gibt es noch keine belastbare Grundlage für eine Zuordnung, ohne den tatsächlichen Migrations-Diff einzusehen — dort steht bewusst "offen", nicht geraten.

## Bereits mit einem PR verknüpft (Build now)

| SR | Kurzform | PR | Status | Beleg |
|---|---|---|---|---|
| SR-02 | Learner reicht Selbstauskunft (field_capture) ein | PR #1 | Schema + Zugriffsregel vorbereitet, Review ausstehend | Access-Matrix-Zeile „Field capture — Rohinhalt/Antworten", RLS-Test 002_learner_own_captures.test.sql (grün in CI). Update 2026-09-14 (PR #7): Anwendungscode-Seite gegen echte Supabase-dev-Daten getestet, dabei field_capture-Insert-Bug gefunden (fehlendes `phase`, NOT-NULL-Verletzung seit Migration 0011) und behoben — gefunden per Code-Review in der Claude-Code-Session, Live-Verifizierung (DB-Zeilen-Inspektion, Screenshots) durch Vera. Kein neuer SR-Fortschritt über den Bugfix hinaus behauptet, Review zu PR #1 weiterhin ausstehend |
| SR-03 | Nur Admin/Instructor darf submitted → verified/rejected setzen | PR #1 | Schema + Zugriffsregel vorbereitet, Review ausstehend | Access-Matrix-Zeile „Verification" |
| SR-06 | Keine organization_id-übergreifenden Datensätze | PR #1 (Basis bereits auf main aus dem Erstentwurf) | RLS-Test grün in CI (001_employer_isolation.test.sql), noch nicht gegen dev verifiziert (dev-Merge steht aus) | CI-Lauf PR #2, `All tests successful` |
| SR-07 | Bestätigte Anwesenheit/Verifizierung unveränderlich | PR #1 | Trigger-Logik entschieden (fn_derive_field_job_status u.a.), Review ausstehend | Team-Entscheidung 2026-09-11 zum Status-Herleitungs-Trigger. Update 2026-09-14 (PR #7): Zusammenspiel mit dem Anwendungscode gegen echte Supabase-dev-Daten getestet, dabei field_job-Update-Bug gefunden (schrieb noch direkt auf `status`, seit Migration 0012 nicht mehr gegrantet — Trigger übernimmt das) und behoben — gefunden per Code-Review in der Claude-Code-Session, Live-Verifizierung (DB-Zeilen-Inspektion, Screenshots) durch Vera. Kein neuer SR-Fortschritt über den Bugfix hinaus behauptet, Review zu PR #1 weiterhin ausstehend |
| SR-08 | Feld-Workflow-Writes klein und atomar | PR #1 | Schema (field_capture als atomarer Write) vorbereitet | Notion-Entscheidungslog 2026-09-09 |
| SR-09 | Binärer Nachweisstatus je Teilschritt-Typ | teilweise PR #1 (Praxis-Teilschritt), Rest ggf. schon im Erstentwurf auf main | Nicht abschließend geprüft — Content-/Live-Anteil dieses SR liegt evtl. schon im Erstentwurf, aber ungetestet | — |
| SR-25 | Learner sieht Nachweistyp + nächsten Schritt im Feld | PR #1 (Schema), UI fehlt noch | Schema vorbereitet, Screen existiert noch nicht in der Next.js-App | Bestätigungs-Screen „erledigt/Problem" laut PR-#1-Beschreibung neu, App insgesamt noch nicht ans neue Schema angepasst. Update 2026-09-14 (PR #7): Bestätigungs-Screen „erledigt/Problem" gegen echte Supabase-dev-Daten getestet, dabei o. g. field_job-Status-Bug gefunden und behoben — gefunden per Code-Review in der Claude-Code-Session, Live-Verifizierung (DB-Zeilen-Inspektion, Screenshots) durch Vera. Kein neuer SR-Fortschritt über den Bugfix hinaus behauptet, Review zu PR #1 weiterhin ausstehend |
| SR-13 | Learner sieht Tagesansicht mit allen geplanten Aktivitäten, chronologisch mit Typ-Kennzeichnung | Commit f3ce821 (2026-09-10, direkt auf main, kein PR/Review durchlaufen) | Funktional verifiziert am 2026-09-14 gegen echte Supabase-dev-Daten (Login, RLS, Anzeige eines Praxistag-Eintrags mit Icon+Label, entspricht design-specifications.md Abschnitt 2.2) — laut Vera manuell geprüft, außerhalb der Claude-Code-Session. Noch offen: formale Review/PR-Nachziehung, da der Commit vor Einführung von CI/Review-Pflicht entstand. Nur der Praxistag-Pfad (field_job) visuell geprüft, lesson/live_session-Pfade im Code vorhanden, aber noch ohne Testdaten verifiziert | Screenshot 2026-09-14 (Vera, manuell). Zusätzlich in der Claude-Code-Session 2026-09-14 verifiziert: `npm run lint` und `npx tsc --noEmit` gegen main grün (nach `node_modules`-Neuinstallation), `/login` liefert HTTP 200 ohne Fehlermeldung im Markup — kein vollständiger End-to-End-Test mit echten Zugangsdaten durch Claude selbst |
| SR-01 | Kompetenzstatus ausschließlich aus Teilschritt-Abschluss berechnet, kein manuelles Schreiben möglich | — (Struktur bereits im Erstentwurf, vor Einführung des PR-Workflows) | Strukturell erfüllt, verifiziert am 2026-09-15 (Schema-/Code-Review in der Claude-Code-Session, keine Live-Daten-Prüfung) | `competency_evidence` ist eine berechnete View (`0006_progress_and_evidence.sql`, `security_invoker = true`) ohne Insert/Update/Delete-Grant für irgendeine Rolle — entsteht ausschließlich aus `unit_progress`/`verification`; kein Formularfeld/API-Endpunkt schreibt den Kompetenzstatus direkt |
| SR-04 | Teilschritte und Belege als N:M-Zuordnung gespeichert | — (Struktur bereits im Erstentwurf, vor Einführung des PR-Workflows) | Strukturell erfüllt, verifiziert am 2026-09-15 (Schema-Review in der Claude-Code-Session, keine Live-Daten-Prüfung) | `content_competency_mapping` (`0004_qualification_structure.sql`, `unique(lesson_id, competency_step_id)`) — ein Teilschritt kann mehrere Lektionen referenzieren und umgekehrt, ohne Duplizierung. Praktische Teilschritte analog über `field_capture_step_mapping` (Group 4) |
| SR-14 | Learner sieht/nutzt Content-Bibliothek seines Qualifikationsprogramms | Commit f3ce821 (2026-09-10, direkt auf main, kein PR/Review durchlaufen) | Strukturell vorhanden, im Rahmen der Schedule-/Kompetenzen-Verifizierung am 2026-09-15 geprüft | `getContentLibrary()` filtert nach `programme_id`, RLS liefert nur `status='published'`-Content. Dev-Server-Log 2026-09-15 zeigt echte Nutzung gegen Supabase-dev-Daten: `GET /content 200`, `POST /content/[lessonId] 200` mit erfolgreichem `markLessonComplete(...)` — von Vera live geklickt |
| SR-15 | Learner sieht Datum, Uhrzeit und Beitritts-Link je geplanter Live-Session | Commit f3ce821 (2026-09-10, direkt auf main, kein PR/Review durchlaufen) | Strukturell vorhanden, im Rahmen der Schedule-/Kompetenzen-Verifizierung am 2026-09-15 geprüft (Code-Review, kein Live-Session-Datensatz live durchgeklickt) | `content/[lessonId]/page.tsx` zeigt bei `content_type='live'` Datum/Uhrzeit + `join_link`-Button. Bekannte Einschränkung: `live_session`-Sichtbarkeit hängt an `schedule_entry.cohort_id`, nicht `enrolment_id` (siehe access-matrix.md, Anmerkung 2026-09-15 zu Seeding-Fehlerquelle) |
| SR-16 | Ausschließlich Magic-Link-Login, kein Passwort | Commit f3ce821 (2026-09-10, direkt auf main, kein PR/Review durchlaufen) | Erfüllt, verifiziert am 2026-09-15 | Code-Review: `login-form.tsx`/`actions.ts` nutzen ausschließlich `signInWithOtp` (`shouldCreateUser: false`), kein Passwort-Feld/`signInWithPassword` im gesamten Code. Dev-Server-Log 2026-09-15 zeigt einen tatsächlich abgeschlossenen Magic-Link-Login: `POST /login 200` (`sendMagicLink`), `GET /auth/callback?code=... 307`, danach authentifizierter Zugriff auf `/schedule` — von Vera live durchlaufen |
| SR-19 | AfCJ admin legt neuen Betrieb + Mitarbeitenden-Accounts an | — | Kein Bauaufwand — über Supabase-Dashboard/Terminal-Prinzip abgedeckt (siehe design-specifications.md §3, Ergänzung 2026-09-15) | Admin-Aktion ist reine Dateneingabe (Supabase Table Editor / Terminal-Session mit Claude), keine eigene Admin-UI nötig für die aktuelle Konstellation (2 AfCJ-Admins, Kohorte bis ~11 Lernende) |
| SR-20 | AfCJ admin legt Kohorte an, ordnet Learner zu | — | Kein Bauaufwand — über Supabase-Dashboard/Terminal-Prinzip abgedeckt (siehe design-specifications.md §3, Ergänzung 2026-09-15) | Admin-Aktion ist reine Dateneingabe (Supabase Table Editor / Terminal-Session mit Claude), keine eigene Admin-UI nötig für die aktuelle Konstellation (2 AfCJ-Admins, Kohorte bis ~11 Lernende) |
| SR-21 | AfCJ admin legt Live-Session (Datum/Uhrzeit/Link/Kohorte) an | — | Kein Bauaufwand — über Supabase-Dashboard/Terminal-Prinzip abgedeckt (siehe design-specifications.md §3, Ergänzung 2026-09-15) | Admin-Aktion ist reine Dateneingabe (Supabase Table Editor / Terminal-Session mit Claude), keine eigene Admin-UI nötig für die aktuelle Konstellation (2 AfCJ-Admins, Kohorte bis ~11 Lernende) |
| SR-22 | AfCJ admin ruft betriebsübergreifende Programm-/Kompetenz-Übersicht ab | — | Kein Bauaufwand — über Supabase-Dashboard/Terminal-Prinzip abgedeckt (siehe design-specifications.md §3, Ergänzung 2026-09-15) | Übersicht ist per SQL-Abfrage (SQL Editor / Terminal-Session mit Claude zur Datenauswertung) abbildbar, keine eigene Admin-UI nötig für die aktuelle Konstellation (2 AfCJ-Admins, Kohorte bis ~11 Lernende) |
| SR-23 | AfCJ admin markiert Repository-Datei als Wissensquelle + Freigabestatus | — | Kuratierungsteil erfüllt (kein Bauaufwand, siehe Prinzip design-specifications.md §3), vollständiges Akzeptanzkriterium abhängig von SR-42 (Build next) | Markieren/Freigeben einer `knowledge_source`-Zeile ist reine Dateneingabe, RLS bereits vorhanden (`knowledge_source_admin_all`, `0009_rls_policies.sql`). Der Teil "wird durchsucht und bei passender Frage zitiert" hängt am noch nicht gebauten Guidance-Antwortsystem (SR-42) — siehe requirements.md-Präzisierung 2026-09-15 |
| SR-48 | AfCJ admin legt Learner-Profil mit Basisfeldern an | — | Kein Bauaufwand — über Supabase-Dashboard/Terminal-Prinzip abgedeckt (siehe design-specifications.md §3, Ergänzung 2026-09-15) | Admin-Aktion ist reine Dateneingabe (Supabase Table Editor / Terminal-Session mit Claude), keine eigene Admin-UI nötig für die aktuelle Konstellation (2 AfCJ-Admins, Kohorte bis ~11 Lernende) |
| SR-55 | AfCJ admin lädt Dateien in zentrales Repository (file_asset) | — | Kein Bauaufwand — über Supabase-Dashboard/Terminal-Prinzip abgedeckt (siehe design-specifications.md §3, Ergänzung 2026-09-15) | Upload ist über Supabase Storage/Table Editor abbildbar, `file_asset` bereits mehrfach referenzierbar (siehe SR-55-Analyse 2026-09-15), keine eigene Admin-UI nötig für die aktuelle Konstellation |

## Noch offen, kein PR zugeordnet

Alle übrigen SRs aus `requirements.md` (Build now: SR-05, 17, 50–52, 57; komplett Build next und Later): noch nicht begonnen bzw. keine gesicherte Zuordnung zu einem bestehenden PR möglich. Wird ergänzt, sobald ein PR sie explizit benennt — laut `CLAUDE.md` muss jeder PR „seine SR-xx" nennen, das macht diese Tabelle ab jetzt weitgehend selbstaktualisierend, wenn ihr das im PR-Text konsequent einhaltet.

Update 2026-09-15 (SR-Triage): SR-01, 04, 14, 15, 16, 19, 20, 21, 22, 23, 48, 55 aus dieser Liste entfernt — haben jetzt eigene Zeilen oben. SR-12 und SR-31 nach `requirements.md` „Build next" verschoben, tauchen deshalb nicht mehr in der „Build now"-Aufzählung auf. SR-13 war schon vorher fälschlich noch in der alten Aufzählung genannt (Zeile war seit PR #6 nicht mehr aktuell) — jetzt korrigiert.

Randnotiz: PR #2 (CI-Pipeline) ist reine Infrastruktur ohne eigenes SR — passt zur Regel „jedes Feature verweist auf ein SR", da CI kein Feature für Learner/Admin ist, sondern Werkzeug für Schritt 6. Nicht als Lücke werten.

## Wöchentliches Update

Bei jedem PR-Review (Definition of Done, Punkt 5–6): Zeile für die im PR genannte(n) SR-xx ergänzen oder aktualisieren, Status auf „gemerged, RLS grün gegen dev" heben, sobald das zutrifft.
